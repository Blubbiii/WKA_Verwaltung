import { useTranslations } from "next-intl";

/**
 * Convenience alias for next-intl's useTranslations hook.
 * Usage: const t = useT('namespace') or const t = useT()
 */
export const useT = useTranslations;
